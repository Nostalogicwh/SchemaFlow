# v0.5.3
## 代码重构优化
1. 前端补充注释
2. 前后端代码检查重构

## 功能修复
1. 点击工作流，会莫名其妙自行执行（有两个不可见的按钮）
2. 检查前后端字段是否对齐

## 优化
1. 日志文件管理
2. 国际化
3. 前后端去掉无意义的检查、if else等处理策略

## 新增
1. 一键部署脚本
2. 用户干预，可以通过配置，在某些情况（比如已保存网站状态时）选择性跳过；但当跳过后的下一个节点执行失败时，支持回退到用户干预节点